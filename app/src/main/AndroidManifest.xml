<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="io.keepcoding.mvvmarchitecture">
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MVVMArchitecture"
        tools:ignore="ExtraText">
        <activity
            android:name=".ui.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.MVVMArchitecture.NoActionBar"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <meta-data
            android:name = "AMADEUS_API_KEY"
            android:value = "$(AMADEUS_API_KEY)"
            />
        <meta-data
            android:name= "AMADEUS_API_SECRET"
            android:value = "$(AMADEUS_API_SECRET)"
            />
        <meta-data
            android:name="IMAGES_API_KEY"
            android:value="$(IMAGES_API_KEY)"
            />
        <meta-data
            android:name="IMAGES_API_SECRET"
            android:value="$(IMAGES_API_SECRET)"
            />
        <meta-data
            android:name="GEOLOCATION_API_KEY"
            android:value="$(GEOLOCATION_API_KEY)"
            />
        <meta-data
            android:name="GEOLOCATION_API_SECRET"
            android:value="$(GEOLOCATION_API_SECRET)"
            />
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="${MAPS_API_KEY}" />
        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version"
            />
    </application>

</manifest>